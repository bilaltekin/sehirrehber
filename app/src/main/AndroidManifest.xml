<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sehirrehberi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/diyarbakir_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/diyarbakir_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.KurumlarActivity">

        </activity>
        <activity android:name=".activity.YemekActivity" />
        <activity
            android:name=".activity.TarihiMekanActivity"
            android:label="@string/title_activity_tarihi_mekan"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>